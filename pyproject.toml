[project]
name = "sohail-studio"
version = "0.1.0"
description = "Local-first AI engineering workspace for Sohail-Agent-CLI"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0.0",
]

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "W"]
